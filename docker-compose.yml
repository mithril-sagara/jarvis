services:
  ollama:
    image: ollama/ollama:latest
    container_name: jarvis-brain
    volumes:
      - ./ollama_data:/root/.ollama
    entrypoint: /bin/sh
    command: -c "ollama serve & sleep 5 && ollama pull llama3.2:1b && wait"
    restart: always

  stt:
    # ARM64(Pi 5)に完全対応し、HTTP APIを提供するイメージに変更
    image: onerahmet/openai-whisper-asr-webservice:latest
    container_name: jarvis-stt
    environment:
      - ASR_MODEL=tiny
      - ASR_LANGUAGE=ja
    ports:
      - "9000:9000"
    restart: unless-stopped

  tts:
    image: voicevox/voicevox_engine:cpu-ubuntu20.04-latest
    container_name: jarvis-tts
    restart: unless-stopped

  jarvis-core:
    build:
      context: .
      dockerfile_inline: |
        FROM python:3.11-slim
        # 1. OSパッケージのインストール
        RUN apt-get update && apt-get install -y \
            alsa-utils x11-xserver-utils xscreensaver portaudio19-dev \
            gcc python3-dev libatomic1 ca-certificates build-essential \
            fontconfig && \
            rm -rf /var/lib/apt/lists/*
        
        WORKDIR /app
        
        # 2. pip基盤の構築
        RUN pip install --no-cache-dir --upgrade pip setuptools wheel
        
        # 3. 依存ライブラリのインストール
        RUN pip install --no-cache-dir "numpy<2"
        RUN pip install --no-cache-dir requests influxdb-client ollama
        RUN pip install --no-cache-dir pyaudio
        RUN pip install --no-cache-dir openwakeword
        
        COPY . .
        CMD ["python", "main.py"]
    container_name: jarvis-core
    depends_on:
      - ollama
      - stt
      - tts
    devices:
      - "/dev/snd:/dev/snd"
    volumes:
      - .:/app
      - ./models:/app/models
      - /tmp/.X11-unix:/tmp/.X11-unix
      - ./models:/usr/local/lib/python3.11/site-packages/openwakeword/resources/models
      - /usr/share/fonts:/usr/share/fonts:ro
    environment:
      - INFLUXDB_URL=${INFLUXDB_URL}
      - INFLUXDB_TOKEN=${INFLUXDB_TOKEN}
      - INFLUXDB_ORG=${INFLUXDB_ORG}
      - INFLUXDB_BUCKET=${INFLUXDB_BUCKET}
      - OLLAMA_HOST=ollama:11434
      # ★STTのURLを新しいコンテナのAPIエンドポイントに変更
      - STT_URL=http://stt:9000/asr?task=transcribe&language=ja&output=json
      - TTS_URL=http://tts:50021
      - SPEAKER_ID=13
      - OLLAMA_MODEL=llama3.2:1b
      - DISPLAY=:0
      - JARVIS_MODEL_PATH=/app/models/jarvis.tflite
      - LANG=ja_JP.UTF-8
    restart: unless-stopped
